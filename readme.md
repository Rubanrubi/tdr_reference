## TDR

```
MySQL Version : 5.7

Update the MySQL credentials in config\database.php
```

```$xslt
Todo multiple language suport
// set selected language locale in session
$request->session()->put("locale",$request->lang);

// add middleware localization
```
